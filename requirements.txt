Django==5.1.3
asgiref==3.11.1

# Realtime WebSocket (Cursor.dev style collaboration)
channels==4.3.2
channels_redis==4.3.0
daphne==4.2.1

# Production server
gunicorn==23.0.0

# Static files production
whitenoise==6.11.0

# Redis (for channel layers production scaling)
redis==7.1.1

# WebSocket backend dependencies (auto required by channels/daphne)
Twisted==25.5.0
autobahn==25.12.2
txaio==25.12.2
zope.interface==8.2
